# Build/deps
node_modules/
dist/

# vitest --coverage report (regenerated by `npm run coverage`)
coverage/

# Asset pipeline (spa/scripts/process-mj-assets.sh)
.venv-assets/
mj-output/

# Live per-OS VM credentials — real passwords, never publish.
# Provide locally by copying src/data/credentials.example.ts (the
# scripts/ensure-credentials.mjs prebuild step does this automatically).
src/data/credentials.ts
