# Amiga 1000 — the first Amiga

Status: **LIVE on the box (dark-launched)** since 2026-09-09 —
streamhost@a1000 host-native on udp/54188, floppy-boot Workbench 1.2 desktop
and XTEST pointer both proven on the production rig. `enabled: true` in the
registry but not yet given a `listing` block for the grid; promote by
reviewing `/os/a1000` and dropping one in.

**Guest:** an emulated **Commodore Amiga 1000** (Motorola 68000 @ 7.16 MHz,
OCS chipset — Agnus/Denise/Paula, 512 KB chip RAM) booting **Workbench 1.2**
(1986) from floppy — the machine as it shipped: the A1000 has no Kickstart in
ROM, only a WCS (write-control store) that a boot ROM loads from a Kickstart
disk on every power-up. **HOST-NATIVE from day one** per rule 13: **FS-UAE
3.2.35 (the station's own pinned source build, patched with the
`amigaos35` mousehack re-arm) runs directly on labhost**, rendered into a
pinned Xvfb `:88` sized to the emulator window and captured with the
daemon's generic `SH_CAPTURE=x11` backend; input is the `x11test` backend
(XTEST absolute motion + paced buttons + keyboard) built for `amigaos35`.

> Distinct from `amiga` (A500 / Workbench 1.3 **bridge kiosk**, not
> host-native) and `amigaos35` (A4000/040, AGA, OS 3.5 — the end of the
> classic 68k line). This is the beginning of it: the 1985 machine with the
> designers' signatures moulded inside the case.

## Identity and source

- Public ID / tile directory: `a1000`
- Reserved slot / UDP port: `188` / `54188`
- Archetype: `beige-tower-crt` (ideal: the actual A1000 case with the monitor
  on top and the keyboard garage underneath)
- Media (staged under `/data/vms/sandbox/a1000/media/`, hash-verified,
  **never committed**):
  - `Kickstart v1.2 r33.180 (1986-10)(Commodore)(A500-A1000-A2000)[!].rom` —
    262 144 B, sha256 `87cddb1f499e32758de20145e73031a84bab299e3f6e5c8487e76d02b2ee9d16`.
    Sits in the ROM slot (see Emulator decision — this is the proven route,
    not the authentic bootstrap).
  - `Amiga 1000 ROM Bootstrap (1985)(Commodore)(A1000)[!].rom` — 65 536 B,
    sha256 `c67d47d0ff4a4cd29104e96fe7920a2ec13dff4f7f0fbcf7be645e2ae493262c`
    (the genuine A1000 boot ROM; staged for the bootstrap route, not shipped).
  - `Kickstart v1.2 r33.166 (1986-09)(Commodore)(A1000)[!].rom` —
    262 144 B, sha256 `86a0de3a6e390fe0804e8d8e3d39091552f3f5020d8bd4989a77bb996bbc50b7`
    (A1000-specific Kickstart, also tried in the bootstrap race).
  - `Workbench v1.2 rev 33.56 (1987)(Commodore)(A500)(Disk 1 of 2)(Workbench).adf`
    — 901 120 B, sha256 `4dfd92a4589346f157593d1a3098966b43e2298c46b749c689b9dc457480be4c`
    (DF0 work copy each boot).
  - `Workbench v1.2 rev 33.56 (1987)(Commodore)(A500)(GB)(Disk 2 of 2)(Extras).adf`
    — 901 120 B, sha256 `7edba9fcbffacde7f1bf94920b76c57e2002ae07b877f26de152ae96c631ba69`
    (DF1 work copy each boot).
  - Source: archive.org items `commodore-amiga-firmware` and
    `commodore-amiga-operating-systems-workbench`.
- License class: preservation-source, copyrighted (Commodore/Amiga). Fine to
  hold and run in this private, passkey-gated collection; never redistribute
  the bits.

## Emulator decision

FS-UAE — the shape sibling of `amigaos35`, whose x11-runtime launcher, Xvfb
capture and XTEST input this station copies wholesale
(`stations-registry.py new a1000 --like amigaos35`); the retronet cage is
NOT copied — a 1986 guest predates the web and ships with no network plane.
Built as the station's OWN pinned copy (`FSUAE_STATION=a1000
build-fsuae-native.sh` → `assets/a1000/fsuae-native/`, never shared with
`amigaos35`/`amix`), carrying the same lab mousehack-rearm patch.

- `--amiga_model=A1000` (OCS, 68000, 512 KB chip — FS-UAE routes this to
  `chipset_compatible A1000` / `cs_a1000ram` in `config-model.c`).
- Window / X screen: 720x568 (`xwininfo`, same as `amigaos35`; a PAL
  640x256 Workbench 1.2 desktop renders inside it).

## Pinned machine (acceptance)

- `fs-uae 3.2.35` (pinned source build, patched), A1000 model, 512 KB chip
  RAM, OCS.
- Storage: no HDF. DF0 = Workbench 1.2 rev 33.56 (Disk 1), DF1 = Extras 1.2
  (GB, Disk 2) — both fresh work copies of the golden ADFs, reflinked on
  every launch.
- ROM slot: the 262 144 B Kickstart 1.2 r33.180 image (the proven route —
  see Open items for the authentic path).
- Display: windowed (never SDL fullscreen, same trap as `amigaos35`),
  geometry `720x568` — the Xvfb root == the emulator window, no letterbox.
- Input: `x11test` backend — XTEST absolute motion, buttons, keyboard;
  `SH_BTN_MIN_HOLD_MS=60`, `SH_KEY_MIN_HOLD_MS=40`, `SH_KEY_MIN_GAP_MS=40`
  (see Open items — the hold value is a lower bound, not daemon-proven).
- Audio: off (`SH_AUDIO=off`).
- Networking: none — pre-web guest, no retronet plane at all.

## Ready scene / golden

- Ready state: Workbench 1.2 desktop (blue, 640x256 PAL in the 720x568
  window) — RAM Disk + "A500 WB 1.2" + Extras disk icons on the right,
  title bar "Workbench release 1.2.  364736 free memory", no windows open.
- Reset mode: `relaunch`, `FSUAE_NATIVE_CHECKPOINT=0` — **no statefile**.
  Every launch is a deterministic cold boot from fresh copies of the
  Workbench and Extras ADFs. Measured at ~55 s to the desktop, both with
  `amigaos35`'s binary (before the station's own was staged) and again with
  the station's own build (confirmed via `/proc/<pid>/exe`) — identical
  desktop up by +55 s both times; golden + binary + device set is the
  proven combination.
- **Bake note:** unlike `amigaos35`'s FFS-root HDF trap, there is no disk
  image to keep clean here — DF0/DF1 are re-copied from the golden ADFs on
  every boot, so there is nothing to "dirty" between resets.

## Input proof

- **Pointer PASS.** An instant XTEST press+release (bare `xdotool click`)
  is never sampled by FS-UAE's mousehack poll — tried five times, zero
  registrations. `mousedown` + 150 ms hold + `mouseup` DOES register (an
  icon selects on mousedown alone), and a 150 ms-hold/150 ms-gap
  double-click opened the "A500 WB 1.2" disk window (free memory
  364736→364272) whose zoom gadget maximizes it to show the
  System/Utilities/Demos drawers.
- **Reset PASS.** Kill by pidfile + relaunch returns the identical pristine
  desktop (364736 free, no windows) in one more boot cycle.
- **Keyboard UNVERIFIED.** No Shell/CLI was reached inside the golden
  stream's 20-minute window (double-clicking into System only registered a
  single click before time ran out) — no typed-character proof exists yet.
  This is an open item, not a known-bad result.
- Proof frames on the box under `/data/vms/streamhost/stations/a1000/proof/`:
  `f050.png`, `ownbin_055.png` (own-binary desktop-up proof), plus the
  golden stream's `mdown.png`/`mup.png`/`dblclick.png`/`zoom.png`/
  `reset_desktop.png`.

## Build and rollback

- Builder: `scripts/build-guests/tiles/a1000.sh` (fetch + sha256 + stage the
  floppy set and ROMs into `assets/a1000/` and the station `disk/`), plus
  `FSUAE_STATION=a1000 build-fsuae-native.sh` for the station's own pinned
  `fs-uae` binary.
- Credentials reference only (never values): `guest/a1000` (Workbench 1.2
  has no login; entry records any config sentinels).
- Rollback: keep the pre-change media set until a replacement is
  restore-proven (golden + binary + device set are ONE combination, same
  rule as every other station).

## Open items

- **Authentic Kickstart-from-floppy bootstrap does not boot in this build.**
  The real A1000 has no Kickstart ROM — a 64 KB (or 8 KB ALTROM) boot ROM in
  the ROM slot loads Kickstart from a floppy in DF0 into the WCS. Every
  combination tried (full 65536 B dump, the 8192 B ALTROM slice, a
  131072 B doubled dump to close the FS-UAE `read_kickstart` FIXME gap, and
  swapping in the A1000-specific r33.166 Kickstart) reset-loops: the
  framebuffer stays flat grey and `fs-uae.log` shows the checksum
  alternating between two states in a steady 1:2 rhythm — the WCS write
  latch (`a1000_handle_kickstart`, `src/memory.cpp`) is firing, so the
  bootstrap does read the Kickstart disk and hand over, but whatever lands
  in the WCS never executes; the machine hard-resets and repeats forever.
  Full trace: `/data/vms/sandbox/a1000/smoke/BOOTSTRAP-FINDINGS.md`. Next
  two theories, in priority order:
  1. Dump `kickmem_bank.baseaddr[0..0x40000]` after the first WCS write and
     diff it against the raw r33.180 file — if the bytes differ, the
     ADF_KICK disk-synthesis offset in `disk.cpp` (`tid->offs =
     i*512*num_secs - 512`) is delivering the image shifted by the "KICK"
     sector header, and the bootstrap is executing garbage.
  2. The 1:2 rhythm suggests a *soft* reset is also re-triggering
     `a1000_reset()` (`devices.cpp:71` calls it unconditionally); patch it
     to fire only on a hard reset in a sandbox build and re-run the race rig.
  The launcher already carries the switch
  (`FSUAE_NATIVE_A1000_BOOTSTRAP=1`, default `0`) for whichever theory
  lands — flipping it is a launcher change, not a re-bake, since reset is
  already a cold relaunch.
- **Keyboard is unverified end to end** (see Input proof) — a runner needs
  to reach a Shell/CLI and type through the daemon's real path, not just
  XTEST-direct-to-window.
- **`SH_BTN_MIN_HOLD_MS=60` is unproven against the daemon.** The 150 ms
  numbers above were measured with XTEST straight to the fs-uae window,
  bypassing the daemon — a lower bound, not a daemon-side measurement. If
  the live station drops clicks in the browser, raise toward 150 before
  touching the launcher.
- Not yet promoted to the visible grid — pending the operator's eyeball at
  `/os/a1000` and a `listing` block.
