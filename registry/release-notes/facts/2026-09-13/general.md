# general — facts from week ending 2026-09-13 ("The Apple II most people used")

Material not tied to one station.

## From the summary prose

- (New stations) Both load in a couple of seconds from an emulated hard disk behind a CFFA card, and a keypress returns to the menu at any time.
- (New stations) It runs host-native from day one, the same MAME driver family as the museum's Taiwanese MPF-II clone, with no bridge machine anywhere underneath it.
- (New stations) A modern coda is planned too: a 2023 fan port of **Angry Birds** for the same 128 KB hardware, waiting only on a working copy of the disk image.
- (New stations) Pac**, **Splat!** and **The Secretary**, a period word processor, all loaded from a single floppy disk image built to look period-authentic.
- (New stations) It too is host-native MAME with no bridge underneath.
- (Major features) Safari 26 gained a native streaming write path: the gallery's video pipeline now offers `datagrams.createWritable()` alongside the older writable-stream fallback it already used everywhere else, so the newest Safari drives the same low-latency path Chrome and Firefox have had for months instead of falling back to a slower one.
- (Major features) The walk-in cells used for signed-in visitors also picked up a shared client-debug plane, so a session that misbehaves on someone's own laptop can be inspected the same way a developer's own test session already could, without asking them to reproduce it twice.
- (Quality improvements) The posters fact-checking pass kept going: two more period write-ups were checked line by line against primary sources, catching a wrong chip name and adding the Signetics 2513 character generator's own history — the chip that drew text on an early Apple II and, through a Philips/Signetics collaboration, on the BBC Micro's teletext decoder too.
- (Quality improvements) Thirteen of fifteen posters are now checked and merged.

(No bullets outside a1000.md / a3000.md / apple2e.md / atari800xl.md / samcoupe.md —
the three Amiga/Apple/SAM/Atari bullets that named a machine in plain text
rather than a `station:` link were moved to a1000.md and a3000.md by hand.)
