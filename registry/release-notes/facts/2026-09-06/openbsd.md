# openbsd — facts from week ending 2026-09-06 ("The mouse finally lands")

Extracted mechanically from the in-progress/append-only week JSON; raw material
for the Sunday authoring pass, not publishable prose on its own.

## Bullets

- [OpenBSD](station:openbsd) installs unattended over a loopback HTTP set server straight into an fvwm desktop; its tablet pointer needed one InputClass fix after Xorg registered the same device twice, once with no calibration range
