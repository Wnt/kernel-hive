# bootos — facts from week ending 2026-09-06 ("The mouse finally lands")

Extracted mechanically from the in-progress/append-only week JSON; raw material
for the Sunday authoring pass, not publishable prose on its own.

## From the summary prose

- (New stations) At the other extreme, [bootOS](station:bootos) is an entire operating system in **512 bytes**, the one sector a PC reads to start up: a prompt, a filesystem and a hex loader, written by Óscar Toledo G. in two evenings in 2019.

## Bullets

- [bootOS](station:bootos) keeps one file per floppy track, thirty-two at most; the museum snapshots the floppy, so a deleted game is gone only until the next reset
