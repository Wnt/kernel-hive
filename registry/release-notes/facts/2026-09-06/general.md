# general — facts from week ending 2026-09-06 ("The mouse finally lands")

Material not tied to one station.

## From the summary prose

- (New stations) Three machines arrived.
- (New stations) It boots from the real 1992 installation tape onto an emulated *68030*, and runs in colour on the **A2410**, a card most owners never bought.
- (New stations) Its cursor lands exactly where you point, by a route no other machine here uses: the museum reaches inside the guest's own X server and moves the pointer there.
- (New stations) Its floppy carries nineteen more one-sector programs — chess, a Doom, a BASIC, a Flappy Bird — and you run one by typing its name.
- (New stations) That makes 80 machines, 76 of them open to visitors.
- (Major features) <u>Five more machines now put the cursor exactly where you point.</u> A mouse in a browser sends a position; most of these guests only understand motion, which is why a cursor used to crawl behind your hand and pile up in a corner.
- (Major features) On all five the cursor is now 1:1: no chase, no drift, no corner.
- (Quality improvements) The private machines handed out to signed-in visitors left everyone else on a blank front page; the museum now renders one hall for all and picks what you see by who you are.
- (Quality improvements) Clicks, not just movement, take the corrected route into AIX, so a button press lands where the cursor already is.
- (Quality improvements) AIX also had an invisible magnet — pass near a window's resize handle and the pointer stuck to it — which is gone.
- (Quality improvements) And the museum's own weekly test used to count cards on the collection page, quietly passing while the page was empty; it now checks the gallery really drew.

## Bullets

- **OPEN LOOK** is the desktop Sun and AT&T backed against **Motif**; Amiga UNIX is the only machine here that runs it
- Type `enter` on bootOS and it takes a program as lines of hex; the 'Hello, world' from its own manual is the exhibit's demo button
- A new tool finds the cursor in a captured frame, which is how each of these loops was proved rather than assumed
- Amiga UNIX seemed stuck in monochrome because a search tool had been silently skipping the one file that proved the emulator could drive its colour card
- SuSE Linux 6.4 joins the gallery: KDE 1.1.2 on kernel 2.2, installed by SuSE's own graphical **YaST2**, from the last release before the small u disappeared
