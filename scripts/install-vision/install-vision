#!/usr/bin/env bash
set -euo pipefail

HERE=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
PYTHON=${VISION_PY:-"$HERE/.venv/bin/python"}
[ -x "$PYTHON" ] || { echo "vision venv missing: run $HERE/install.sh" >&2; exit 1; }
exec "$PYTHON" "$HERE/flow.py" "$@"
