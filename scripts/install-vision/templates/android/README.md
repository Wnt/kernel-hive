# Android reference crops

These crops come from the task's Android-x86 9.0-r2 framebuffer, not from a
web image. `find_template.py` searches them over a scale range so they remain
usable when the framebuffer changes resolution. Each crop includes the full
button glyph and a small, visually stable margin.
