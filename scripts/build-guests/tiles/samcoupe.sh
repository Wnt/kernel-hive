#!/bin/bash
# samcoupe media builder — STUB written by the spine (2026-09-08); the media stream
# replaces this with the pinned fetch + SHA-256 + host-side disk composition
# (docs/lab/SAMCOUPE-WAVE.md). Owned by the media stream.
set -euo pipefail
echo "samcoupe: media builder not written yet (TODO(media))" >&2
exit 1
