#!/bin/bash
# atari800xl media builder — STUB written by the spine (2026-09-08); the media stream
# replaces this with the pinned fetch + SHA-256 + host-side disk composition
# (docs/lab/ATARI800XL-WAVE.md). Owned by the media stream.
set -euo pipefail
echo "atari800xl: media builder not written yet (TODO(media))" >&2
exit 1
