"""The stations-registry generator, split from the former monolithic
scripts/stations-registry.py (terminology stage 2 shim still points at it as
the entry point). See scripts/stations-registry.py for the CLI entry.
"""
