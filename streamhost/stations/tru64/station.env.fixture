# ---- CHECKPOINT FIXTURE (x11 runtime tile) ----
# The install is done and the station resets through a CHECKPOINT: the
# launcher reflink-copies assets/tru64/checkpoint/tru64.img and restores
# checkpoint/tru64.axp, the es40 savestate baked from that same image
# (serial-menu option 5 = save-and-exit, so the pair is atomic). A reset lands
# the CDE desktop ~3 s after exec instead of the ~7-10 min SRM -> rc -> CDE
# cold boot. Deleting assets/tru64/checkpoint/ is the whole rollback — the
# launcher cold-boots img/tru64-seed.img on its own, no edit needed.
SH_RESET_MODE=relaunch
SH_FIXTURE_DESC=Tru64 UNIX 5.1B (CDE desktop, autologin root, 1024x768) in es40 (AlphaServer ES40, EV68); SH_CAPTURE=shm (headless SDL dummy, no X server) + mamesock ctlsock input; relaunch restores the golden checkpoint in ~3 s.

# ---- POINTER/KEY INPUT (SH_* read by the daemon; the socket served by es40) --
# mamesock = the daemon speaks mamectl/1 straight to the ctlsock the es40 fork
# serves (src/gui/ctlsock.h, armed by the launcher via ES40_CTL_SOCK). This
# tile's es40 build is MULTI-CLIENT: the daemon stays attached while operator
# tooling (ctltest.py) injects keystrokes beside it. Keyboard verified
# end to end at the SRM console (typed command echoed, framebuffer proof
# 2026-08-11). Pointer is open-loop absolute like w2kalpha; the X11 installer
# has not had a pointer calibration pass — keyboard is the drive channel.
SH_INPUT_BACKEND=mamesock
SH_MAMECTL_SOCK=/data/vms/streamhost/stations/tru64/ctl.sock

# ---- IDLE AUTO-PAUSE: ON since 2026-08-16 (the w2kalpha stanza) ----
# The install phase kept this OFF (a SIGSTOP'd installer makes no progress and
# a mid-setld freeze risks corruption on resume). The seed exists now, so this
# station behaves like its sibling: the guest stays POWERED ON and is
# SIGSTOPped at ~0 CPU once no visitor has been connected for
# SH_IDLE_PAUSE_SECS, then resumed sub-second on the next visit. That — not a
# savestate — is what "instant" means on the es40 stations; the fork carries
# fc82f05 host_freeze_reanchor so guest clocks resume where they stopped.
# SH_IDLE_PAUSE_PROC_MATCH is the anti-footgun: the signal only ever goes to a
# pid whose cmdline is this station's own es40. Rollback is one line:
# SH_IDLE_PAUSE_SECS=0. (Since 2026-08-16 the two mechanisms compose: the
# station RESTORES a checkpoint at launch and is SIGSTOPped between visits.)
SH_IDLE_PAUSE_SECS=60
SH_IDLE_PAUSE_PIDFILE=/data/vms/streamhost/stations/tru64/mame.pid
SH_IDLE_PAUSE_PROC_MATCH=assets/tru64/es40
# Warmup withholds the FIRST freeze. It was 540 s because a cold boot needed
# ~400-450 s to reach CDE and a mid-boot freeze would strand an unvisited
# station part-booted. The checkpoint restore reaches the finished desktop in
# ~3 s, so 60 s is ample — and it still covers the first phase of a cold-boot
# fallback (a freeze there is slow, not fatal: the gap is absorbed on thaw).
# If this station is ever rolled back to cold boot for good, put it back
# to 540.
SH_IDLE_PAUSE_WARMUP_SECS=60
