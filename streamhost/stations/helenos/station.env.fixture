# --- GOLDEN FIXTURE (see golden-manifest.json) ---
SH_RESET_MODE=loadvm
SH_GOLDEN_SNAPSHOT=golden
SH_GOLDEN_DISK=/data/vms/streamhost/stations/helenos/golden.qcow2
SH_GOLDEN_RESET_CMD=loadvm golden
SH_GOLDEN_MANIFEST=/data/vms/streamhost/stations/helenos/golden-manifest.json
# Fixture: 1024x768 HelenOS compositor; focused Bdsh Terminal at prompt "/ # helenos"
# (non-blinking block cursor = keyboard-reactive); clear blue desktop + Start menu =
# mouse-reactive; pointer parked on the Terminal title bar. No screensaver/DPMS exists.
# Only dynamic region: taskbar clock seconds, rect x[982..1010] y[747..756] -> MASK it.
# --- CANARY baseline (feat/streamhost-coldboot-latency @71998d9) ---
