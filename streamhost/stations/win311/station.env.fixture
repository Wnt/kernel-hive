
# --- GOLDEN TEST FIXTURE (added by golden-fixture bake) ---
# resetMode=loadvm : disks are standalone qcow2, snapshot "golden" holds live RAM+disk.
# Reset a test to the fixture with:  python3 drive.py qmp.sock loadvm golden
SH_RESET_MODE=loadvm
SH_GOLDEN_SNAPSHOT=golden
SH_GOLDEN_DISKS=win311-golden.qcow2,games-golden.qcow2
# Fixture (2026-09-15 recapture): clean WfW 3.11 Program Manager desktop, ALL
# icon groups cascaded open, NO application window on top — a visitor chooses
# what to launch. Screensaver OFF, caret blink quieted (CursorBlinkRate=65535),
# no idle animation. See GOLDEN.md.
# History: an earlier bake left Notepad maximized+focused; the 2026-08-17
# re-bake moved to Program Manager + Gallery Games; the 2026-09-14
# www.mrshowbiz.com home-page recapture (6e8ea366) left Netscape AND IE3 open
# on top of Program Manager (both apps used to set/verify the new home page,
# never closed before capture) — operator-reported as "no open application in
# golden scene" and fixed 2026-09-15 by Alt+F4-closing both windows on the
# live station before recapture; no guest autostart config (WIN.INI load=/run=
# only carries AGENT.EXE + runonc16.exe, no browser) caused it.
