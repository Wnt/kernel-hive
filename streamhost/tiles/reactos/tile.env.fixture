
# --- golden test-fixture reset metadata (added by fixture bake) ---
# Curated deterministic fixture: ReactOS LiveCD -> clean desktop, active empty
# Notepad (focused caret = keyboard surface), pointer on clear desktop + Start
# button (mouse surface), taskbar clock hidden, screensaver/DPMS/power-off off.
GOLDEN_RESET_MODE=loadvm
GOLDEN_SNAPSHOT=golden
GOLDEN_SNAPSHOT_STORE=/data/gallery-guests/ReactOS/reactos-golden.qcow2
GOLDEN_MANIFEST=/data/gallery-guests/ReactOS/golden-manifest.json
# Reset live (no restart): QMP human-monitor-command 'loadvm golden' on $SH_QMP
