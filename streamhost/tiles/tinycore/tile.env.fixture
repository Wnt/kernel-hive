
# ---- GOLDEN FIXTURE (see golden.env / golden.json for the machine-readable manifest) ----
# resetMode=loadvm  |  reset action: QMP 'loadvm golden' (live, no reboot) -> golden-reset.sh
# fixture: clean TinyCore FLWM desktop + aterm terminal open, STEADY non-blinking caret,
#          keyboard-reactive prompt; wbar dock + clear desktop = mouse-reactive surface.
# tweaks: screensaver off (xset s off/noblank), DPMS off (std-VGA not DPMS-capable), no idle anim.
# golden snapshot 'golden' lives in state.qcow2 (scratch virtio qcow2 attached only to hold it).
# NEVER delete state.qcow2. Launch auto -loadvm golden when the snapshot exists.
