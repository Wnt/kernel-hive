
# --- Golden test fixture (baked 2026-07-06) ---
# Fixture: Administrator autologon -> clean desktop; Notepad focused (fixed text,
# solid non-blinking caret) as input-reactive surface; screensaver off; monitor/
# standby/disk power-down = Never; taskbar clock hidden. Full detail + reset steps
# in golden-manifest.json. Tile runs WITHOUT -snapshot; reset live via QMP loadvm.
SH_RESET_MODE=loadvm
SH_GOLDEN_SNAPSHOT=golden
SH_GOLDEN_MANIFEST=/data/vms/streamhost/tiles/win2000/golden-manifest.json
