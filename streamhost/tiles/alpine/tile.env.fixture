
# ---- GOLDEN FIXTURE (see golden.env / golden.json for the machine-readable manifest) ----
# resetMode=loadvm  |  reset action: QMP 'loadvm golden' (live, no reboot) -> golden-reset.sh
# fixture: clean Alpine root text console + banner, STEADY caret, keyboard-reactive prompt.
# tweaks: console blank OFF, VESA powerdown OFF, fbcon cursor-blink OFF (no idle animation).
# golden snapshot 'golden' lives in state.qcow2 (scratch IDE qcow2 attached only to hold it).
# NEVER delete state.qcow2. Launch auto -loadvm golden when the snapshot exists.
